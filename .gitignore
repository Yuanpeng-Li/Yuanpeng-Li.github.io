# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ignore the Gemfile that is generated
Gemfile.lock

# Ignore locally installed gems
vendor/
.bundle/

# Ingore files created by npm
node_modules
package-lock.json

# Local debug log
tea_debug.log

# Cloudflare visitor-tracker Worker source (deployed separately, kept local only)
tracker/